# Импорт протоколов — см. [README.md](README.md)

Весь GAS в папке `scripts/google-apps-script/` (файл `import-protocols.gs` + `Menu.gs`).

Деплой: `.\scripts\deploy-gas.ps1`

Формат JSON: [`docs/protocol/`](../../docs/protocol/).
